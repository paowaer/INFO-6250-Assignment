# SO MUCH - onClick

```
<button 
    onClick={() => setCount((count) => count + 1)}
>
    count is {count}
</button>
```

Let's simplify to better understand:

```
<button onClick={() => setCount(count + 1)} >
    count is {count}
</button>

```
